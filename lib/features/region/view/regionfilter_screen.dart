import 'package:flutter/material.dart';

class RegionFilterScreen extends StatelessWidget {
  const RegionFilterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Region Filter")),
      body: const Center(child: Text("Region Filter Feature Coming Soon")),
    );
  }
}
